const inputA: string | null = prompt("The first number?", "");
const inputB: string | null = prompt("The second number?", "");

const a1: number = Number(inputA);
const b: number = Number(inputB);

alert(a1 + b);
